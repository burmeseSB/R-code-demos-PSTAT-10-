View(CO2) # views the dataset of CO2
uptake_vec <- c(CO2 $ uptake) # CO2 $ uptake calls the uptake vector from dataset, assing the var name uptake_vec
mean(uptake_vec) #find average of values in uptake vector

